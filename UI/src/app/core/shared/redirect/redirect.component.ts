import { Component } from '@angular/core';

@Component({
  standalone: true,
  selector: 'app-redirect',
  template: '',
})
export class RedirectComponent {

  // marker component used for routing - don't remove

}
